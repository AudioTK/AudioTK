
FILE(GLOB
    ATK_UTILITY_SRC
    *.cpp
)

FILE(GLOB
    ATK_UTILITY_HEADERS
    *.h*
)

ATK_add_library(ATK_UTILITY
  NAME ATKUtility
  FOLDER Utility
  INCLUDE ${FFT_INCLUDES}
  LIBRARIES ${FFT_LIBRARIES}
  SRC ${ATK_UTILITY_SRC}
  HEADERS ${ATK_UTILITY_HEADERS}
)
