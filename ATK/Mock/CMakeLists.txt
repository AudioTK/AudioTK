
FILE(GLOB
    ATK_MOCK_SRC
    *.cpp
)

FILE(GLOB
    ATK_MOCK_HEADERS
    *.h*
)

if(ENABLE_TESTS)
  ATK_add_library(ATK_EQ
    NAME ATKMock
    FOLDER Mock
    DEFINITIONS "-DBOOST_ALL_NO_LIB"
    INCLUDE ${FFT_INCLUDES}
    LIBRARIES ATKUtility ATKCore ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY}
    SRC ${ATK_MOCK_SRC}
    HEADERS ${ATK_MOCK_HEADERS}
  )
endif(ENABLE_TESTS)
